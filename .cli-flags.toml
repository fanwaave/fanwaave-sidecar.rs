# flags-2-env surface for `fanwaave-sidecar`
[identity]
service = "fanwaave-sidecar"
type_name = "SidecarEnv"

[help]
url = "https://github.com/fanwaave/fanwaave-sidecar.rs"
columns = ["options", "env", "type", "default", "description"]

[parse]
allow_unknown = false

[flags.bind]
env = "FANWAAVE_SIDECAR_BIND"
long = "bind"
type = "string"
default = "127.0.0.1:9090"
help = "HTTP listen address for /healthz /readyz /metrics."
